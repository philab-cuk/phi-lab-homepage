# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright MCP scratch (browser snapshots, console logs)
.playwright-mcp/
# Local visual-verification screenshots (kept on disk for the user, not committed)
*-desktop.png
*-mobile.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local settings (per-machine)
.claude/settings.local.json

# react-router framework build
build/
.react-router/

# supabase CLI local state
supabase/.branches/
supabase/.temp/
test-results/

# 미사용 원본 소스 이미지(최적화본만 배포)
public/배경이미지_은하수.png
